# Shared Markdown Renderer

Hello **bold** and *italic* text with [safe](HTTPS://Example.COM/Path) and [unsafe](javascript:alert(1)).

Click <kbd>Enter</kbd> to continue.

1. Ordered item
2. Next item

> Quote with `inline code`

```rust
fn main() {
    println!("hi");
}
```
